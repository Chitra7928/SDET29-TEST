package com.crm.GenericLibrary;

import java.io.File;
import java.io.IOException;

import org.apache.commons.io.FileUtils;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.support.events.EventFiringWebDriver;
import org.testng.ITestListener;
import org.testng.ITestResult;

//public class ListImp implements ITestListener {
	
	//public void onTestFailure(ITestResult result) {
		
	
		
		//String failedTestName =result.getMethod().getMethodName(); //capture 
		//System.out.println("******Listener*********");
		//EventFiringWebDriver eDriver = new EventFiringWebDriver(BaseAnnotationClass.sDriver);
		//File srcFile = eDriver.getScreenshotAs(OutputType.FILE);
		//try {
			//FileUtils.copyFile(srcFile, new File("./Screenshot/"+failedTestName + ".png"));
		//} catch (IOException e) {
		
			//e.printStackTrace();
		